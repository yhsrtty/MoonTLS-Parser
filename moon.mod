name = "yhsrtty/moontls-parser"

version = "0.1.1"

readme = "README.md"

repository = "https://github.com/yhsrtty/MoonTLS-Parser"

license = "Apache-2.0"

keywords = [ "tls", "ja3", "ja4", "fingerprint", "security", "parser" ]

preferred_target = "wasm-gc"

description = "TLS ClientHello parser and JA3/JA4-style fingerprint toolkit for MoonBit traffic-audit probes."

import {
  "moonbitlang/x@0.4.45",
}
